<!DOCTYPE html>
<html lang="uk">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>FilmForToday - Агрегатор Кінотеатрів</title>
    <link rel="stylesheet" href="/css/styles.css" />
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css" rel="stylesheet">
</head>
<body>
    <main>
        <%- include('partials/header') %>
        <div class="container">
            <%- include('partials/search') %>

           <section class="top-offers">
                
                <div class="top-offers-header">
                    <div class="top-offers-title">
                        <h2>Топ пропозиції</h2>
                        <p>Фільми з найвищим рейтингом IMDb</p>
                    </div>
                </div>
                
                <div class="carousel-wrapper">
                    
                    <button id="carousel-prev" class="carousel-arrow" aria-label="Назад">
                        <i class="fas fa-chevron-left"></i>
                    </button>
                    
                    <div class="carousel-container" id="top-offers-carousel">
                        <% if (locals.topMovies && topMovies.length > 0) { %>
                            <% topMovies.forEach(movie => { %>
                                <a href="/film/<%= movie.id %>" class="movie-card">
                                    <img src="<%= movie.image %>" alt="<%= movie.name %>">
                                    <h3><%= movie.name %></h3>
                                </a>
                            <% }); %>
                        <% } else { %>
                            <p>Наразі немає топ-пропозицій.</p>
                        <% } %>
                    </div>
                    
                    <button id="carousel-next" class="carousel-arrow" aria-label="Вперед">
                        <i class="fas fa-chevron-right"></i>
                    </button>
                    
                </div> </section>
        </div> 
    </main>

    <%- include('partials/auth-modals') %>
    <div id="toast-container"></div>
    <script src="/js/main.js"></script>
    <script src="/js/search.js"></script>
    <script src="/js/home.js"></script>
</body>
</html>